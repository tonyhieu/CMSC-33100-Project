# CMSC-33100-Project


Duncan's work so far:

I added a custom class defining what we want our simulated job to consist of. Now it just has the timestamp when it was submitted, an expected length and an actual length

I added a script to simulate these jobs and export them into a pickled file that can be loaded by our test framework